#pragma once

class App
{
public:
    // run the application
    static int run();
    // initialize the application
    static int init();
    // cleanup the application
    static int cleanup();
};
